// without using JQuery
tips('.targets').create({
        width: 200,
        edgeOffset: 10,
        action: 'hover',
        content: 'fromDom',
        float: 'auto'
    });
// all parameters can be omitted (only for example)

tips('#eg').create();
